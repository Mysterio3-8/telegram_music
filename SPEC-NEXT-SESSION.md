# СПЕКА НА СЛЕДУЮЩУЮ СЕССИЮ

Собрано по итогам сессии 2026-07-23…08-01. Здесь всё, о чём договаривались,
что сделано, что осталось, и **чем начинать**. Цифры — снятые с прода 01.08.2026,
не оценки.

---

## 🔴 ПРИОРИТЕТ 1 — Бот отвечает 20 секунд

**Симптом владельца:** «бот долго откликается на команды, 20 секунд надо ждать».

### Замеры прода (01.08.2026, 12:01)

```
load average: 3.63, 2.47, 1.58     ← при 1-2 ядрах это перегрузка в 2-3 раза
Mem: 961 МБ всего / 845 занято / available 116 МБ   ← на грани OOM
Диск: 20 ГБ, занято 13 ГБ (68%)
```

### Что выяснено про причины

1. **На боксе одновременно крутится 7+ Python-процессов** (каждый держит свой
   интерпретатор + SQLAlchemy + aiogram, это 50-200 МБ на процесс):
   `tg-music-bot`, `tg-music-api`, `tg-music-worker`, `tg-music-soundcloud`,
   `tg-music-youtube-user`, `tg-music-telegram-channel`, `tg-music-support`
   — плюс **чужой проект** `news-rewriter` на том же сервере.
   Один процесс (celery) ел **20.8% всей памяти**.

2. **Celery-воркеры конфликтуют именами.** В логе:
   `DuplicateNodenameWarning: Received multiple replies from node name:
   celery@a519558306.local`. Все юниты запускают celery **без флага `-n`**, поэтому
   у 4 воркеров одинаковый nodename. Лечится добавлением `-n %h-<очередь>` в каждый
   юнит (`deploy/tg-music-*.service`).

3. **`tg-music-youtube-scan.service` в состоянии failed** — разобрать причину
   (`journalctl -u tg-music-youtube-scan -n 50`).

4. **`tg-music-worker` был в auto-restart** — перезапускается по кругу, вероятно
   OOM-kill. Проверить `journalctl -u tg-music-worker | grep -i kill`.

5. **SSH-брутфорс жрёт CPU** — см. приоритет 2: `btmp.1` на 114 МБ неудачных
   входов. Поставить fail2ban и отключить парольный вход. Это часть load average.

### Что уже сделано в этой сессии (не помогло до конца)

- SQLite → **WAL** + `busy_timeout=30000` (устранило «database is locked»)
- `build_task_engine()` — PRAGMA теперь применяются во всех 7 файлах задач
- антиспам-мидлварь (гасит флуд до обращения к БД)
- глобальный обработчик ошибок

### План на следующую сессию

1. **Замерить, где именно уходят секунды.** Добавить в мидлварь замер времени
   обработки апдейта и логировать всё, что дольше 1 сек, с указанием хендлера.
   Без этого оптимизация — гадание. (Файл: `app/middlewares/`, новый `timing.py`.)
2. **Уникальные nodename** для celery-юнитов (`-n %h-soundcloud` и т.д.).
3. **Сократить число процессов**: объединить редко используемые очереди
   (`telegram_channel` + `worker`) в один воркер, снизить concurrency.
4. **Проверить индексы БД** под горячие запросы бота: `search_index` уже есть,
   проверить `EXPLAIN QUERY PLAN` для `search_tracks` на 30к треков.
5. **Кардинальное решение — переезд на сервер побольше** (см. приоритет 3).
   Текущий бокс (961 МБ RAM, 20 ГБ диск, общий с чужим проектом) физически не
   тянет: музыка качается 24/7, база 30к треков.

---

## 🔴 ПРИОРИТЕТ 2 — Диск забивается

**Симптом владельца:** «надо решить проблему с диском, чтобы он не забивался».

### Что занимало место (снято 01.08.2026)

```
/var/log                     1.2 ГБ   ← НЕ journald! разбор ниже
/opt/tg-music-bot/backups    646 МБ   ← бэкапы БД, ротация BACKUP_KEEP=2
/opt/tg-music-bot/audio_cache 509 МБ  ← LRU-кэш стрима (AUDIO_CACHE_MAX_MB)
/opt/tg-music-bot/music_bot.db 323 МБ ← сама база
/opt/tg-music-bot/storage    268 МБ   ← архивные копии треков
/opt/tg-music-bot/Проверка    16 МБ   ← мусор: скрины залиты в репо
/opt/tg-music-bot/копи       3.8 МБ   ← мусор
/opt/tg-music-bot/скриншоты  2.6 МБ   ← мусор
/tmp                         5.5 МБ   ← чистится таймером, в порядке
```

Разбор `/var/log` (важно — `journalctl --vacuum` освободил **0 байт**, дело было
не в journald):

```
/var/log/syslog      350 МБ
/var/log/syslog.1    229 МБ
/var/log/journal     193 МБ
/var/log/btmp.1      114 МБ   ← ⚠️ лог НЕУДАЧНЫХ входов = SSH-брутфорс
/var/log/kern.log    104 МБ
/var/log/atop         60 МБ
/var/log/auth.log*    62 МБ
```

### ✅ Сделано в конце сессии

- Удалены ротированные архивы, активные логи обнулены (`truncate`), atop старше
  3 дней вычищен → **освобождено ~1 ГБ**: было 13 ГБ занято / 6.2 ГБ свободно,
  стало **12 ГБ / 7.1 ГБ (63%)**
- **journald ограничен постоянно**: `SystemMaxUse=150M`, `MaxRetentionSec=7day`
  в `/etc/systemd/journald.conf`, демон перезапущен

### ⚠️ Главная находка — сервер брутфорсят по SSH

`btmp.1` на 114 МБ — это сотни тысяч неудачных попыток входа. Отсюда же
распухшие `auth.log` и `syslog`. Это и лишняя нагрузка на CPU (частично объясняет
load average), и риск безопасности.

**Что сделать в следующей сессии:**
1. Поставить **fail2ban** (или `sshguard`) — банит IP после N неудачных попыток.
2. Отключить вход по паролю: `PasswordAuthentication no` в `/etc/ssh/sshd_config`
   (ключи уже используются — вход по ключу работает).
3. Опционально: сменить порт SSH с 22 на нестандартный — убирает 90% ботов.

### Осталось по диску

1. **Убрать из репозитория папки со скринами** (`Проверка/`, `копи/`,
   `скриншоты/`, `проверка/`) — в `.gitignore` и удалить с прода (~22 МБ).
2. **Настроить logrotate** для syslog/kern.log (сейчас растут до сотен МБ между
   ротациями): `/etc/logrotate.d/rsyslog` → `size 50M`, `rotate 3`.
3. **Автоочистка по порогу**: расширить таймер `tg-music-catalog-maintain`
   (уже чистит `/tmp` старше 2 часов) — добавить контроль `audio_cache` и
   `backups` при свободном месте меньше N ГБ.
4. **Мониторинг**: предупреждение админу в бот при свободном < 2 ГБ
   (есть `services/disk.py` с `enough_free_disk()` — переиспользовать).
5. Вынести бэкапы **offsite** — сейчас 646 МБ лежат на том же сервере, потеря
   сервера = потеря бэкапов.

---

## 🟡 ПРИОРИТЕТ 3 — Переезд на нормальный сервер (корень проблем 1 и 2)

Текущий бокс: **961 МБ RAM, 20 ГБ диск, делится с проектом news-rewriter**.

Это первопричина: и тормозов, и заполнения диска, и падений (см. инциденты
2026-07-26/27 в `CLAUDE.md` — переполнение диска уронило Redis и бота).

Что даст переезд:
- нормальная работа при 30к+ треков и растущей базе
- возможность вернуть агрессивный парсинг (сейчас `discover_loop` и
  `recover_covers` **запрещены** на этом боксе — уронят)
- PostgreSQL вместо SQLite (шаг №1 в `SCALING.md`)
- полный архив треков вместо LRU-кэша

**Именно на это собираются 29 ₽ подписки** — так и написано в посте для канала.

---

## ✅ ЧТО СДЕЛАНО В ЭТОЙ СЕССИИ (не переделывать)

### Поиск и парсеры
- **Поиск по любой букве/автору, кириллица**: `tracks.search_index`
  (нижний регистр + транслит), миграция `0e5ddc09e7e6`, бэкфилл 30 148 треков.
  Корневой баг: SQLite `lower()`/`ILIKE` понижают только ASCII — «ки» русскими
  буквами находило 0. Проверено на проде: «к» → 18 740, «кизару» → 240, «би» → 2151.
  Бэкфилл: `python -m app.cli.reindex_search`
- **Парсеры разведены**: массовый 24/7 — **только SoundCloud**
  (`attach_source_for_artist` не заводит YouTube → `no_source`);
  поисковый — SoundCloud, YouTube фолбэком. Чистка: `research disable-youtube-sources`
- **Всегда mp3** для поискового парсера (`download_soundcloud_audio(as_mp3=True)`),
  оригинальная обложка
- **Фильтр «только треки»**: `search_min_seconds=60`, `search_max_seconds=900`.
  Подобрано по реальной базе через `python -m app.cli.duration_stats`
  (<60 сек — 10% обрезков, >15 мин — 4,3% подкастов; режет ~14% мусора).
  Длительность SC берётся из выдачи → мусор отсекается **до** скачивания
- **Качество выдачи**: штраф пере-версиям (slowed/reverb/cover), если их не
  просили явно; автор аплоада учитывается (официальный трек вместо реаплоада)
- **Выдача в боте списком**: кнопки «2:19 Артист - Название» + пагинация,
  тап → аудио (`handlers/quick_search.py`, `keyboards/quick_search.py`)

### Деньги
- **29 ₽/мес** вместо 49 ₽, тарифы 1/3/6/12 (29/87/174/348)
- **Stars скрыты** из интерфейса (код приёма оставлен)
- **Триал 1 день**, пэйвол Mini App (`miniapp/src/screens/paywall.js`)
- Админам `is_premium_active` всегда True (иначе владелец упрётся в свой пэйвол)
- `/start` переписан, кнопка «Открыть плеер — 29 ₽/мес»
- Перенос музыки: кнопка убрана из бота, осталась в Mini App

### Устойчивость
- Глобальный обработчик ошибок `handlers/errors.py`
- Антиспам `middlewares/throttling.py` (0.7 сек интервал, 12 действий/10 сек,
  пауза 20 сек, предупреждение один раз на серию, админы без лимитов)
- SQLite WAL + busy_timeout + `build_task_engine()`

### Каталог (ранее в сессии)
- 324 жанра с иерархией, чипы в поиске, экран жанра
- Карточки артистов: фото, баннер, жанры, последний релиз, синглы, топ, альбомы,
  похожие артисты, подписка («Мои артисты»)
- MusicBrainz-исследователь: **5142 артиста**, 12 255 привязок жанров
- `tracks.artist_id` + бэкфилл, автоплейлисты по жанрам
- Поиск секциями (Артисты/Альбомы/Плейлисты/Треки)
- Шит трека: в плейлист, в очередь, следующим, к артисту

**Тестов: 352, все зелёные.**

---

## 🔍 РАЗОБРАННЫЙ БАГ: «аудио играет ускоренно»

**Вывод: причина не на сервере.** Проверено (не догадки):

| Проверка | Результат |
|---|---|
| Фильтры темпа в коде (`atempo`, `asetrate`) | нет ни одного, grep по `app/` пуст |
| Частота файлов | 44100 Гц у **всех 40** проверенных |
| Длительность в БД ↔ фактическая | 179↔179.8, 190↔190.8, 60↔60.1 — совпадает |
| `playbackRate` в Mini App | не используется |

Отдельно проверена версия «48000 против 44100» (даёт ровно +8,8% — «немного, но
заметно») — **опровергнута**, все файлы 44.1 кГц. Если бы файл был ускорен, его
длительность стала бы короче — она совпадает до десятых.

**Что делать:** в плеере Telegram есть регулятор скорости (1x/1.5x/2x), он
запоминается глобально и применяется ко всем аудио. Открыть трек → выставить **1x**.
⚠️ **Владелец должен подтвердить**, помогло ли. Если нет — скачать файл из бота и
сравнить побайтово с оригиналом на SoundCloud.

---

## 📋 ОСТАЛОСЬ СДЕЛАТЬ (по убыванию важности)

### Обязательное
1. **Живой платёж 29 ₽ картой** — владелец должен проверить сам. Деньги тестом
   не покрыты. Заодно проверить автопродление (рекуррент).
2. **Скрины для поста** — владелец снимает сам, список из 6 кадров лежит
   комментарием в `NEWS.md` под постом «Пост +1».
3. **Опубликовать посты**: «Пост +2 — Честно про 29 ₽» (личный, первым) и
   «Пост +1 — Бот бесплатный, плеер за 29 ₽» (со скринами).

### Технический долг
4. **Перевод базы в mp3**: сейчас 29 076 m4a + 1 073 mp3. Новые из поиска уже mp3.
   Массовый перевод тяжёлый: файлы живут у Telegram по `file_id`, локально только
   LRU-кэш → надо скачать, переконвертировать, переотправить 29k треков.
   **Делать после переезда на больший сервер.**
5. **PostgreSQL вместо SQLite** (шаг №1 `SCALING.md`) — нужен даунтайм и
   координация с владельцем.
6. **Offsite-бэкапы** — сейчас лежат на том же сервере (646 МБ).
7. **Срез 5б каталога**: таблица `albums` + импорт SoundCloud sets как альбомов
   (сейчас альбомы работают из `tracks.album`).
8. **Конкурсы** доделаны частично: модели, сервис, API, экран, CLI есть;
   не доделаны рассылка и розыгрыш end-to-end.

### Из референсов (папка `проверка/`, 90 скринов разобрано)
Владелец сказал: «Обзор» и «Книги и шоу» **не делаем** (3 таба).
Не сделано и не блокирует:
- радиостанции
- лента друзей (нужен соцграф)
- гранулярные настройки уведомлений
- приватный аккаунт
- дизлайк трека

---

## ⚠️ ГРАБЛИ (не наступать снова)

- **SQLite и кириллица**: `lower()`/`ILIKE` понижают только ASCII. Любой поиск по
  русским буквам — только через `search_index` (нормализованный в Python).
- **MusicBrainz**: 1 запрос/сек **на IP**. Два прогона параллельно запускать нельзя.
- **Долгие прогоны на VPS**: только `setsid nohup … &`. Ждать завершения — по PID
  (`kill -0`), **не** по `pgrep` имени команды: pgrep находит сам вотчер и цикл
  не кончается никогда.
- **Этот бокс не тянет агрессивный парсинг**: `discover_loop` и
  `recover_covers --limit 30000` **не запускать** — дважды роняли прод (инциденты
  2026-07-26/27: диск 100% → Redis не пишет → бот падает).
- **Redis при флуде**: счётчики антиспама держим в памяти намеренно — запись на
  диск при флуде уже роняла прод.
- **Celery**: каждый юнит должен иметь уникальный `-n` (сейчас конфликт имён).

---

## 🚀 С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

```bash
# 1. Снять текущее состояние
ssh news-rewriter-vps 'df -h /; free -m | head -2; uptime; systemctl list-units --state=failed --no-legend'

# 2. Понять, где уходят секунды (сначала измерить, потом чинить)
ssh news-rewriter-vps 'journalctl -u tg-music-bot --since "1 hour ago" | grep -iE "error|slow|timeout" | tail -30'

# 3. Быстрая победа по диску (1.2 ГБ логов)
ssh news-rewriter-vps 'journalctl --vacuum-size=200M'
```

Дальше — по приоритетам 1 → 2 → 3 из этого документа.
