# Что осталось

## ГЛАВНОЕ на следующую сессию — [SPEC-КАТАЛОГ.md](SPEC-КАТАЛОГ.md)

Мировой каталог: все жанры → таблица genres, ~4500 артистов (1000 RU + 3000
Запад + 500 мир) через MusicBrainz/Deezer, настоящие карточки артистов
(баннер/фото/описание/жанры/альбомы), автопоиск ссылок SoundCloud/YouTube,
закачка 3000 треков/сутки. Плюс: прочитать папку **`проверка/`** со скринами
владельца (появится) — сверить дизайн, добить недостающие кнопки/функции.

Прокси (7 шт) уже в `PROXY_LIST` на VPS, ротация работает. Закачка 460
артистов идёт. Архивная политика: вечного архива на диске НЕТ (решение
владельца) — только LRU-кэш (сам чистится) или S3, когда появится.

---

# Архив прошлых заметок (2026-07-21, после второй волны)

CLAUDE.md в корне — карта проекта и оба чекпоинта за сегодня, прочитать первым.
Две волны закрыли: P1 (стрим), P2-инфраструктура (прокси), P3 (главная/поиск/
профиль/ПК), P6 (рассылка), P7 (SCALING.md), P8 (посты), перенос из Spotify/
Яндекс/ВК, ускорение Mini App, эквалайзер/акцент/скачать всё, очередь/таймер
сна/повтор/микс по треку, триал/достижения-награды/лидерборд рефералки.

## Заблокировано — нужно от владельца

| Что | Для чего |
|---|---|
| Список прокси или бюджет на покупку (резидентные/mobile) | Включить `PROXY_LIST` в .env на VPS — инфраструктура уже готова, просто не включена |
| Текстовый список артистов | Таблица `artists` + биндинг треков + чистка Unknown (план ниже) |
| Решение: вход 1 ₽ через ЮKassa или 1 ⭐ Telegram Stars | Монетизация P5.1 |
| Что из остатка дизайна нужно (настройки VK-структурой, онбординг «какие артисты нравятся») | Экраны существуют и работают, просто не в пиксель-копии со скринов |

## Не заблокировано — можно делать сразу

1. **Артисты как сущности** (без списка от владельца можно готовить инфраструктуру):
   таблица `artists` (id, name, normalized_name) + `tracks.artist_id` (nullable), миграция.
   `/artists` переключить на чтение из таблицы. CLI-биндинг по `lower(trim(tracks.artist))`
   ждёт текстового списка — сам биндинг делать рано.
2. **Настройки экрана «как в VK»** (photo_24-25 из копи/): профиль-карточка сверху экрана
   настроек, порядок разделов. Косметика, не блокирована.
3. **Онбординг «Какие артисты вам нравятся?»** (photo_11): выбор ≥5 избранных при первом
   входе — `getFavoriteArtists`/`toggleFavoriteArtist` в prefs.js уже есть, нужен только экран.
4. **Уловки Premium** (P5.2 из старой спеки, ещё не сделаны):
   upsell после отказа в пачечном импорте плейлиста/канала, «Premium до конца дня со скидкой».
   Триал и достижения-награды уже реализованы — это следующий слой поверх них.

## Проверить на реальных данных (не проверялось живьём)

- [ ] **Перенос плейлиста**: взять свою реальную ссылку Spotify или Яндекс.Музыки,
      прогнать `/transfer` в боте — парсинг по ссылке тестировался только на моках.
      Текстовый список (ВК) уже проверен вживую в dev-браузере.
- [ ] **Эквалайзер в реальном Telegram-вебвью** — Web Audio с `createMediaElementSource`
      иногда капризничает на iOS WKWebView; в dev-браузере граф строится и звук меняется.
- [ ] **Рассылка**: `/admin` → 📣 Рассылка → короткий тест на себе (ещё не гонялась в проде).
- [ ] Полный бэкфилл архива отложен осознанно (диск 20 ГБ, свободно 5.8, нужно 6.1) —
      после S3 или увеличения диска: `python -m app.cli.backfill_archives`.

## Владельцу

- [ ] Опубликовать 5 новых постов из [NEWS.md](NEWS.md) (сверху) в канал.
- [ ] Прислать список прокси / текст с артистами / решение по 1₽-Stars, когда будет время —
      без этого три пункта выше не сдвинутся.

## Состояние на конец сессии 2026-07-21 (вторая волна)

- 227 тестов зелёные. Всё запушено и задеплоено на прод, миграции применены,
  сервисы активны, `keybest.cc` отвечает.
- Новые env с дефолтами (трогать `.env` не обязательно): `AUDIO_CACHE_DIR`,
  `AUDIO_CACHE_MAX_MB`, `PROXY_LIST`, `SOUNDCLOUD_MIN_DELAY/MAX_DELAY`,
  `ARCHIVE_MIN_FREE_GB`.
- Новые модели: `user_achievements`, `users.bot_blocked`, `users.trial_used`.
