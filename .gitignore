.venv/
__pycache__/
*.pyc
.env
*.db
# Спутники SQLite. Без них в списке `git add -A` утащил их в репозиторий, и
# следующий git pull на проде отказался обновляться: он собирался перезаписать
# ЖИВОЙ wal-файл работающей базы. Отказался — и правильно сделал, иначе база
# могла поехать.
*.db-wal
*.db-shm
*.db-journal
.pytest_cache/
storage/

# Референсные скриншоты/эскизы дизайна (не код). Уезжали на прод вместе с кодом
# и занимали там 22 МБ на диске в 20 ГБ — держим только локально.
MiniAppPhoto/
kreen/
Проверка/
проверка/
копи/
скриншоты/
скрины, для рекалмы/
фон.png

# Данные прода: рабочие каталоги, которые создаются на месте
backups/
audio_cache/
logs/
