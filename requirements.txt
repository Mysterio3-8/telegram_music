# ⚠️ Не выше 3.28: с 3.29 импорт типов aiogram раздулся — 96 МБ у 3.28,
# 125 у 3.29, 151 у 3.30 на КАЖДЫЙ процесс (бот, поддержка, два воркера).
# Замер на проде 25.09: сервер на 961 МБ жил в свопе, и бот поднимал редкие
# пути (инлайн) секундами. Поднимать версию — только с замером памяти.
aiogram>=3.28,<3.29
SQLAlchemy>=2.0,<3
aiosqlite>=0.20
pydantic-settings>=2.4
alembic>=1.13
mutagen>=1.47
redis>=5.0
celery[redis]>=5.3
boto3>=1.34
yt-dlp>=2024.8
curl_cffi>=0.5  # impersonation для yt-dlp — SoundCloud иначе отдаёт 404 на частых запросах
telethon>=1.36
fastapi>=0.115
uvicorn>=0.30
pyjwt>=2.9
python-multipart>=0.0.9
aiohttp>=3.9  # HTTP-клиент: ЮKassa, LRCLIB (тексты песен)

# прод-БД (когда поднимем PostgreSQL):
# asyncpg>=0.29

# Системные зависимости (не pip): ffmpeg + chromaprint (fpcalc) для аудиоотпечатков
#   Ubuntu: apt install ffmpeg libchromaprint-tools

# тесты
pytest>=8
pytest-asyncio>=0.24
httpx>=0.27  # нужен starlette.testclient (тесты API)
