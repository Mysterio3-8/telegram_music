aiogram>=3.13,<4
SQLAlchemy>=2.0,<3
aiosqlite>=0.20
pydantic-settings>=2.4
alembic>=1.13
mutagen>=1.47
redis>=5.0
celery[redis]>=5.3
boto3>=1.34
yt-dlp>=2024.8
telethon>=1.36
fastapi>=0.115
uvicorn>=0.30
pyjwt>=2.9
python-multipart>=0.0.9

# прод-БД (когда поднимем PostgreSQL):
# asyncpg>=0.29

# Системные зависимости (не pip): ffmpeg + chromaprint (fpcalc) для аудиоотпечатков
#   Ubuntu: apt install ffmpeg libchromaprint-tools

# тесты
pytest>=8
pytest-asyncio>=0.24
httpx2  # нужен starlette.testclient (тесты API)
