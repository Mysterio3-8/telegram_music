# Стресс-тест и поиск багов — runbook

Первый полный проход: **2026-09-13** (коммит `4d26c48`). Цель файла: повторный
запрос «сломай приложение» за полчаса, а не за день. Сначала повтори уже
найденное (регрессия), и только потом ищи новое в местах, изменившихся с даты
выше (`git log --since=2026-09-13 --stat`).

## Правила прохода (решения владельца 13.09)

- **Нагрузка — только локально.** Прод (1 ядро, 961 МБ) уже падал от нагрузки.
  На прод можно только мягкие пробы по HTTPS и чтение журналов.
- Найденное чинить, покрывать тестом и деплоить.
- **Пэйвол Mini App остаётся клиентским** (решение владельца): API Premium не
  проверяет. Это не баг прохода, в отчёт — «известный риск».
- Потолок одного переноса плейлиста — 1000 треков.
- Свайп вниз = «Назад» на вложенных экранах — ТЗ §3 владельца, не трогать.

### Решения владельца 14.09 (Infinity QA v2.0 — бесконечный цикл)

Перекрывают правила 13.09 там, где противоречат.

- **Режим:** итерации в одной сессии; к ~50% контекста всё записано в файлы,
  владелец делает compact, цикл продолжается. Журнал итераций — раздел
  «Журнал циклов» в конце этого файла.
- **Деплой без подтверждения**, но только после полного pytest. Увидел ошибки
  пользователей на проде после выкатки — откат (`git reset` на прошлый
  коммит на сервере + рестарт), потом исправление.
- **Пэйвол закрывается на сервере.** Без Premium в API доступны только вход,
  оплата, профиль, документы. Бот не трогается. Админы и триал проходят.
- **Нагрузка на прод разрешена** — только в ночное окно 03:00–05:00 МСК,
  без рассылки пользователям, короткими заходами; при росте ошибок — стоп и
  подъём сервисов.
- **Сервер не меняем:** выжимаем текущий VPS. PostgreSQL и больший сервер —
  только план в документе.
- **Мониторинг:** ошибки 5xx и исключения — в журнал и сводкой владельцу в
  Telegram; плюс копия на локальный компьютер владельца, пока он включён.

## Быстрый повтор (≈10 минут)

1. Тесты: `.\.venv\Scripts\python.exe -m pytest -q`. Регрессия прохода —
   `tests/test_qa_stress_fixes.py` (22 теста).
2. Стенд: `preview_start api` + `preview_start miniapp` (`.claude/launch.json`).
   Dev-БД `dev_miniapp.db` сначала догнать миграциями:
   `$env:DATABASE_URL="sqlite+aiosqlite:///dev_miniapp.db"; .\.venv\Scripts\python.exe -m alembic upgrade head`
3. Злоупотребления API в обход интерфейса (35 проверок, сам логинится через
   подписанную `initData`, создаёт двух обычных пользователей):
   ```
   $env:DATABASE_URL="sqlite+aiosqlite:///dev_miniapp.db"; $env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe scripts\qa_stress_api.py
   ```
   Ожидание: `багов 0`. ⚠️ Проверка «follow … без сироты» может показать
   `сирот=1` — это строка из первого прогона 13.09 в dev-БД, смотри на `status=404`.
   ⚠️ `transfer` локально отвечает 503/422: брокера Celery нет — потолок и замок
   проверяет unit-тест.
4. Dev-токен для Mini App в браузере (пользователь-админ из dev-БД):
   `create_access_token(5852263277)` → `localStorage["tgmusic-dev-token"]`, плюс
   `localStorage["tgmusic-onboarded"]="1"`.
5. Мягкая проба прода — блок команд ниже.

## Что покрыто и чем проверять

| Область | Проверка | Как |
|---|---|---|
| Гонки | триал ×10, библиотека ×8, профиль ×6 параллельно | скрипт |
| Права | не-Premium пишет текст песни; чужой плейлист (IDOR) | скрипт |
| Границы ввода | название 1 МБ, текст 5 МБ, запрос 1 МБ, 50 000 строк переноса, пустые/пробельные поля загрузки, битый mp3 | скрипт |
| id | 0, отрицательный, шире int64 (API и `/start track_…`/`ref_…`) | скрипт + тест |
| Сироты | трек/артист с несуществующим id | скрипт (SQLite FK выключены!) |
| Накрутка | клики по каналу подписки | скрипт |
| Медиа | Range: `bytes=-N`, мусор, мультидиапазон, за концом файла; поддельная подпись | скрипт |
| Лимитер | 150 запросов/мин с одного IP → 429 + `Retry-After` | скрипт |
| Нагрузка | 300 параллельных `/library/ids` | скрипт (локальная задержка к проду не переносится) |
| Деньги | скидка не сгорает при создании ссылки, гасится при оплате | тест |
| Очереди | перенос не в `youtube_user`, потолок, замок | тест |
| XSS | preview переноса, тост, textarea, результат загрузки, фото в шапке | браузер: `showToast('<img src=x onerror=window.__xss=1>')` → `__xss` не появляется |
| Потеря ввода | ввести в загрузку/перенос → `showToast()` → значение на месте | браузер (`import('/src/state.js')`) |
| Live-трек | в плеере нет кнопок библиотеки/шита/текста у `id:"live:…"` | браузер |
| Адаптив | 375 / 768 / 1280: `scrollWidth == innerWidth`, док не перекрывает последний блок | браузер |
| Быстрые тапы | 60× `playNext()` → запросов `/listen` не больше пары | браузер (перехват `fetch`) |

## Мягкая проба прода (без нагрузки)

```bash
for p in / /src/main.js /nonexistent /.git/config /.env /api/health /api/track/1 /webhook/yookassa; do printf "%-20s " "$p"; curl -s -o /dev/null -w "%{http_code} %{size_download}B %{time_total}s\n" -H "Accept-Encoding: gzip" "https://keybest.cc$p"; done
curl -sI https://keybest.cc/ | grep -iE "^(server|strict|x-content|referrer|cache)"
```

Ожидание: сканерные пути — 404, `Server: nginx` без версии, есть
`Strict-Transport-Security`, `main.js` сжат (~17 КБ). В этой сборке curl нет
`--http2`: HTTP/2 проверять по конфигу (`listen 443 ssl http2`).

## Выкатка исправлений

✅ С 15.09 выкатка — просто `git push origin main`: сервер сам забирает коммит
после зелёного job `tests` (таймер `tg-music-pull-deploy`, раз в 2 мин,
[docs/АВТОДЕПЛОЙ.md](../АВТОДЕПЛОЙ.md)). Журнал решений —
`/var/lib/tg-music-deploy/history.log`. Прошлый SSH-путь из Actions падал во всех
25 прогонах. Ручная выкатка (берёт тот же замок), если нужно без ожидания тестов:

```bash
ssh -i "C:/Users/Илья/.ssh/id_ed25519" -o UserKnownHostsFile="C:/Users/Илья/.ssh/known_hosts" root@38.244.213.132 'cd /opt/tg-music-bot && setsid nohup bash deploy/remote-deploy.sh > /root/deploy.log 2>&1 < /dev/null &'
```

Готово, когда в `/root/deploy.log` появилась строка «✅ Деплой … прошёл».
После выкатки сверить `git rev-parse --short HEAD` на сервере с локальным.

## Итог прохода 13.09

Исправлено 20+ находок — полная таблица с серьёзностью в
[PROJECT-STATUS.md](../ai-engineer-os/PROJECT-STATUS.md) (раздел «Проход
2026-09-13»), урок — в [LESSONS.md](../ai-engineer-os/LESSONS.md).

**Не проверялось (UNKNOWN):** реальные iPhone/Android; задержки API под нагрузкой
на проде; бот под флудом живым клиентом (только существующие тесты антифлуда);
восстановление из бэкапа.

## Куда смотреть в следующий раз первым делом

- Новые эндпоинты API: у каждого ли серверные права, длина и существование id
  (урок 13.09 — интерфейс не сторож).
- Новые `innerHTML` с данными от источников или пользователей — через `escapeHtml`.
- Новые однократные действия — условный `UPDATE` или уникальный индекс.
- Новые долгие задачи Celery — не в `youtube_user` (поток поиска).
- Новые эндпоинты API закрыты пэйволом автоматически проверкой
  `tests/test_server_paywall.py`: открытый маршрут надо явно внести в `OPEN_ROUTES`.
- Всё, что зовётся на КАЖДОЕ действие (мидлвари бота, `ensure_user`,
  `get_current_user`): сколько запросов к БД и есть ли `commit` без изменений.
  Урок цикла 5 — одна безусловная запись на действие хуже десятка чтений.
- Константы уровня модуля с `t(...)`: считаются один раз на языке по умолчанию
  (реклама была по-русски у всех, цикл 4). Текст — только при показе.
- Словари в памяти процесса, ключ — пользователь или IP: у каждого должен быть
  потолок или уборка.
- Файлы/аудио: не читать целиком в память на запрос — отдавать кусками с диска.
- Правка `deploy/remote-deploy.sh` действует со ВТОРОЙ выкатки (bash исполняет
  старую копию). После неё — `pip freeze` и число рестартов в `/root/deploy.log`.
- **CI красный, а локально зелёный** (дважды 16.09): у CI нет `.env` и концы строк LF.
  Воспроизводить так: `git -c core.autocrlf=false clone` в scratchpad → `checkout`
  коммита → pytest тем же venv. Причины были: токен JWT создавался при импорте
  тестового модуля (секрет подставляет conftest позже) и отпечаток сборки по байтам.
- Тест, который обходит «все маршруты» / «все объекты», сперва проверить на
  непустоту (цикл 1: FastAPI 0.141 прятал роутеры в `_IncludedRouter`).

## Журнал циклов

### Цикл 1 — 14.09 (коммит `221373f`, на проде)

- 🔴 **Пэйвол только в интерфейсе** → `require_premium` (402) на всём API,
  кроме входа/оплаты/профиля/языка/гейта/вебхуков/подписанного аудио.
  Проверено на проде: бесплатный `/library/ids` → 402, `/premium/status` → 200.
- ⚠️ Первая версия теста обхода маршрутов **проверяла пустоту**: в FastAPI 0.141
  `app.routes` отдаёт обёртки `_IncludedRouter`, а не `APIRoute`. Разворачивать
  через `original_router`; стережёт `test_route_walk_sees_routers`.
- ⚠️ Бесплатному поверх пэйвола висел гейт подписки «бесплатно, пока подписаны» —
  после подписки он упирался в пэйвол. Гейт для бесплатных в Mini App убран.
- Мониторинг: сторож (раз в 2 мин) пишет ошибки журналов и 5xx nginx в
  `/var/lib/tg-music-health/errors.log` и шлёт в Telegram (антиспам 30 мин).
  Копия на ПК владельца: задача планировщика `InfinityMusic-PullErrors`
  (каждые 10 мин) → `logs/prod/`. Замер до включения: 0 ошибок за сутки.
- Деплой: после рестарта `/health` и 90 сек наблюдения; ≥5 ошибок/5xx → откат.
  ⚠️ Действует со СЛЕДУЮЩЕЙ выкатки: bash исполнял старую копию скрипта,
  которую `git reset` заменил на ходу.
- Скрипт злоупотреблений: 40 проверок (+5 на пэйвол), u2 делается платящим.
  Остаток «follow сирот=1» — старая строка dev-БД, статус 404 верный.
- Замер прода (админ, прогретый): `/library/ids` 5 мс, `/tracks?100` 66 мс /
  22 КБ, `/artists` 58 КБ, `/genres` 70 КБ, `/profile` 40 мс / 17 КБ, `/mix`
  82 мс. ⚠️ Первый запрос после рестарта — **4.3 сек** (холодный кэш страниц
  SQLite при забитом свопе).

### Цикл 2 — 14.09 (коммиты `03d0d2f`, `cfdcf52`, `5835142`)

- ⚠️ **Лимитер по IP резал бы CGNAT**: сотни абонентов оператора на одном адресе,
  120/мин на всех. Теперь ведро на пользователя (JWT), у IP потолок 600/мин;
  чистый ASGI вместо BaseHTTPMiddleware (тот оборачивал каждый аудиопоток).
- 🔴 **Аудио читалось в RAM целиком на каждый Range** (каждая перемотка):
  20 слушателей × 8 МБ ≈ 160 МБ на боксе 961 МБ. Теперь файл с диска кусками
  256 КБ; промах кэша качается из Telegram прямо в файл, одна закачка на трек
  при 10 одновременных «play» (`tests/test_audio_stream.py`).
- `/artists?limit=` (онбординг брал 58 КБ ради 24 имён), `Cache-Control`:
  `/artists` 5 мин, `/genres` час.
- 🔴 **Прод жил на других версиях пакетов, чем тесты**: fastapi 0.139 / starlette
  1.3.1 / aiogram 3.29.1 на проде против 0.141 / 1.6.0 / 3.30.0 локально —
  `pip install -r` без `-U` не обновляет. Введён `requirements.lock` (без yt-dlp
  и curl_cffi), деплой и CI ставят с `-c`, откат возвращает `pip freeze`.
  ⚠️ Замок применится со второй выкатки после `5835142` (первая исполняет
  старую копию `remote-deploy.sh`).
- ⚠️ На проде `.env` и `.env.bak-stars` были **644** (читает любой пользователь
  машины, включая www-data). Все сервисы работают от root → выставлено 600.
  Ещё 7 копий `.env.bak-*` уже были 600.
- Замер прода после `cfdcf52`: трек 7 МБ, 8 параллельных Range-потоков — RSS
  API 118 → 130 МБ (+12 МБ; прежний код держал бы ≥56 МБ только байтами),
  полный файл 19 мс, диапазон 4 мс. Наблюдение деплоя: 0 ошибок, 0 ответов 5xx.
- N+1: `/playlists` и `/curators` считали треки запросом на КАЖДЫЙ плейлист
  (140 плейлистов = 141 запрос) → один сгруппированный `count_tracks_by_playlist`.
- Замок версий проверен на проде `pip install --dry-run -c`: разрешается без
  конфликтов, обновит 20 пакетов (fastapi, starlette, aiogram, uvicorn…).

### Цикл 3 — 14.09

- ⚠️ **Первая тревога сторожа была шумом**: yt-dlp пишет уровнем ERROR обрыв
  соединения SoundCloud («Connection reset by peer»), а человек его не видит —
  поиск уходит в соседний источник. Сбои источников (`SOURCE_NOISE` в
  `healthcheck.sh` и `remote-deploy.sh`) больше не будят владельца и не
  откатывают деплой; отдельная тревога — только при ≥20 сбоях за 2 минуты
  (источник лёг целиком).
- N+1 в живом поиске: сверка «есть ли кандидат в базе» шла запросом на каждого
  из ~30 кандидатов, на каждый поиск и каждую полку → `find_tracks_by_metadata_bulk`
  одним `IN` (`tests/test_live_search_bulk.py`).
- Эта выкатка первой применяет `requirements.lock` на проде (20 пакетов).
- ⚠️ **Каждая выкатка перезапускала сервисы дважды**: `install-units.sh` рестартил
  всё сам, а `remote-deploy.sh` следом ещё раз (Celery дважды рвал закачки).
  Плюс на каждом деплое без изменений: `logrotate -f`, рестарт journald и
  fail2ban. Теперь `SKIP_SERVICE_RESTART=1` из деплоя, системные конфиги
  трогаются только при `cmp`-разнице.
- ⚠️ **Redis без потолка памяти** (`maxmemory 0`), а 01.08 очередь в 651 МБ уже
  положила бокс. `install-units.sh` ставит 256 МБ с `noeviction` (брокер и FSM
  не должны молча терять ключи), сторож тревожит с 100 МБ. Норма — 1.6 МБ.
- Аудит вебхуков: ЮKassa перепроверяет платёж и возврат запросом к API,
  Crypto Pay сверяет подпись по сырому телу — дыр не найдено.
- Снимок прода: Redis 17 ключей, очереди пусты; память youtube-user 182 МБ,
  worker 142 МБ, api 129 МБ, bot 15 МБ; БД 13.7 МБ.

### Цикл 4 — 14.09

- ⚠️ **`/profile` грузится при каждом входе** (и бесплатным под пэйволом), а
  считал статистику дважды и приглашённых трижды (~25 запросов). Теперь один
  проход; серия дней считается `DISTINCT date()` в SQL, а не выгрузкой отметки
  каждого прослушивания в Python (у активного слушателя — десятки тысяч строк).
- `/upload`: файл до 50 МБ целиком в памяти → не больше 2 загрузок разом,
  лишнему 503 + `Retry-After` (10 одновременных = полгигабайта).
- ⚠️ Выкатка `90fa377` снова исполнила СТАРУЮ копию `remote-deploy.sh`: замок
  версий не применён (fastapi 0.139 / starlette 1.3.1 на проде), сервисы
  рестартнулись дважды. Урок: правка деплой-скрипта действует со второй
  выкатки — проверять `pip freeze` после неё.
- ✅ Выкатка `c5b3def` исполнила новый скрипт: замок применён (fastapi 0.141.1,
  starlette 1.6.0, aiogram 3.30.0, uvicorn 0.52.1), каждый сервис перезапущен
  ОДИН раз. Прод: `/profile` 122 мс, `/playlists` 6 мс. Наблюдение: 0 ошибок,
  0 ответов 5xx.
- 🔴 **Реклама в боте всегда по-русски**: `AD_TEXT = t("ads.text")` считался
  при импорте модуля на языке по умолчанию. Англо-, испано-, турецкоязычные
  получали русский текст. Теперь `t()` при показе (язык из ContextVar —
  I18nMiddleware зарегистрирована раньше AdMiddleware).
- ⚠️ Счётчики рекламы (`AdMiddleware._counters`) росли на каждого написавшего
  боту навсегда → потолок 50 000 записей, в словаре хранится остаток от N.

### Цикл 5 — 14.09

- ⚠️ **Гейт подписки — ~4 запроса к SQLite на каждое сообщение и нажатие**
  (пользователь, список каналов, статус по каждому каналу), хотя вердикт не
  менялся с прошлой минуты. Добавлен кэш «подписан» в памяти на 60 сек
  (`services/subscription.py`): только положительный вердикт; принудительная
  проверка (`/start`, «Я подписался») при отписке выбивает запись; добавление
  или удаление канала сбрасывает кэш; Premium-вердикт не кэшируется (истечение
  должно вернуть гейт сразу); потолок 50 000. В тестах кэш чистится autouse-
  фикстурой — тесты переиспользуют `telegram_id=555`.
- ⚠️ Кэш — на процесс: API (Mini App) и бот держат свои копии. Канал, добавленный
  в админке бота, в API начнёт требоваться не позже чем через 60 сек.

### Воронка бота — замер прода 15.09

База (только чтение). 69 пользователей: июль 12, август 40, сентябрь 17; оплат
(`payments`) — **0 за всё время**; 30 пришли по рефералке, из них 26 привёл
владелец. Язык: ru 66, en 3.

Путь новичка: `/start` → выбор языка → гейт подписки (@tgramuzuka) → кабинет
(приветствие, «отправьте название песни», «Открыть плеер», цена).

С 15.08 (32 человека, когда шаг языка уже был):

| Шаг | Прошли | Потеряно |
|---|---|---|
| выбор языка | 31 / 32 | ≈0 |
| гейт подписки | 26 / 31 | 5 (16%) |
| первое действие (поиск, прослушивание, библиотека) | 14 / 26 | **12 (46%)** |

Главная потеря — ПОСЛЕ гейта: подписался, увидел кабинет и не отправил даже
названия песни (поиск в боте пишется в `search_queries` с 07.08). Гипотеза —
первая кнопка «Открыть плеер» ведёт в платный Mini App (0 живых входов за
14.09). **UNKNOWN**: событий шагов нет, куда нажимают — не видно. Порядок
экранов и тексты — решения владельца, без него не меняются.

✅ **Замер шагов включён 15.09** (таблица `funnel_events`, миграция
`f8a9b0c1d2e3`, `app/services/funnel.py`): `start` → `lang_chosen` →
`gate_shown` → `gate_passed` → `cabinet_shown` → `miniapp_login` →
`paywall_hit`, первое прохождение, одна строка на человека и шаг. Отчёт по
когорте пришедших: `python -m app.cli.funnel --days 7`. Смотреть ~22.09, когда
наберётся хотя бы 10–15 новичков; тогда решать, что менять в кабинете.
⚠️ Нажатие кнопки «Открыть плеер» Telegram серверу не сообщает — открытие Mini
App видно только по `miniapp_login`.

✅ **Пробный период 7 дней без пэйвола (владелец 15.09: «чтобы доходили юзеры»).**
`TRIAL_DAYS` 1 → 7; `/premium/status` отдаёт `trial_available` и `trial_days`;
Mini App при старте сам зовёт `POST /premium/trial`, если период доступен, и
открывает приложение с тостом — пэйвол видит только тот, у кого триал уже был.
Сервер по-прежнему выдаёт триал один раз (условный UPDATE). Тексты: кабинет бота
на 7 языках «7 дней бесплатно», пэйвол, главная, баннер Premium, FAQ.
⚠️ Триал — это Premium целиком: 7 дней без обязательной подписки на канал и без
рекламы в боте тоже (так было и с однодневным). 6 человек, уже потративших
однодневный триал, новый не получают. Эффект смотреть по `miniapp_login` в
`python -m app.cli.funnel`.

✅ **Напоминания «продлите Premium бесплатно»** (владелец 15.09: «чтобы больше
было взаимодействий»). Бот раз в сутки (таймер обслуживания, 04:00 UTC,
`python -m app.cli.reminders`, `--dry` — посчитать) пишет в три окна по дате
окончания Premium: осталось 3–4 суток, меньше суток, закончился за сутки. В
сообщении — друзей до ближайшей награды с личной ссылкой и ближайшее
достижение с днями, кнопки «Поделиться ссылкой» (t.me/share) и «Открыть плеер».
Одно сообщение на человека/вид/дату (`user_reminders`, миграция `a0b1c2d3e4f5`);
админам, заблокировавшим бота и автопродлению не пишем; «bot was blocked» →
`bot_blocked`; временный сбой — повтор на следующем запуске. 7 языков. В Mini
App на главной у активного Premium — карточка «Продлите Premium бесплатно» с
теми же цифрами из профиля.

### Аналитика — сбор всего подряд (владелец 15.09)

«Собирать очень много статистики — бизнес, прослушивания, жанры, настроения —
чтобы анализировать и улучшать». Сделано:

- **`analytics_events`** (миграция `9c8d7e6f5a4b`, `app/services/analytics.py`):
  один журнал, белые списки имён и источников (`bot | miniapp | worker | system`),
  свойства JSON до 1 КБ, ошибка записи не ломает сценарий. Жанр и настроение не
  копируются — отчёт джойнит их из треков.
- **Сервер пишет:** `listen`/`download` с источником (бот, Mini App, выдача
  воркером поиска) тем же коммитом, что `track_events`; `search` из бота с
  числом результатов и из Mini App; `reminder_sent` с видом напоминания.
- **Mini App шлёт** (`miniapp/src/analytics.js`, пачкой раз в 8 сек / 50 штук /
  при сворачивании, `POST /analytics/events`, без Premium): `play_complete`,
  `play_skip` с позицией в %, `screen_view`, `paywall_view` (один раз при
  переходе), `share_click` (реферальная ссылка и трек). `listen`/`download` из
  браузера не принимаются — их пишет сервер.
- **Отчёт `python -m app.cli.analytics --days 30`:** всего/новые, активные за
  сутки/неделю/окно, удержание через сутки и неделю (когорта 8–38 дней),
  прослушивания по источникам, дослушивания и пропуски, топ треков, жанры,
  настроения, доля пустых поисков и топ запросов, оплаты (₽, ⭐, платящие),
  донаты, Premium и пробные периоды, показы пэйвола, «поделиться», напоминания и
  возврат за 48 ч. Воронка новичков — отдельно, `python -m app.cli.funnel`.
- ⚠️ Источник прослушивания размечается с 15.09; всё раньше — «до разметки».
  Настроение у треков размечено мало (ставит админ) — жди много «не размечено».
- **Выкачено 15.09 (`ba96e95`), проверено на проде:** миграция `9c8d7e6f5a4b`
  (head), напоминания в таймере обслуживания, `reminders --dry` — к отправке 0
  (активный Premium у одного человека; пробные недели дойдут до окна «3–4 дня»
  позже), `POST /analytics/events` → 204, 5xx после выкатки — 0. Пробное событие
  проверки удалено из базы.
- **Первый отчёт прода за 30 дней:** 69 людей, 32 новых; активны за неделю 3;
  **удержание через сутки 9%, через неделю 3%** (когорта 33); 108 прослушиваний у
  14 человек, все «до разметки»; жанры — хип-хоп 34, trap 31; настроения — 108
  «не размечено»; 65 запросов; оплат 0, донат 1 на 149 ₽.
  ⚠️ В топе треков мусорные имена до 200 символов (рингтон `.m4r`) — в отчёте
  теперь режутся до 60.
- **Доделано вторым заходом:** число результатов поиска из Mini App (лог уходит
  после ответа поиска, упавший поиск — без числа); событие `app_open` и сессии
  (пауза >30 мин = новая; медиана и среднее); `--csv PATH` — сырые события окна
  (utf-8-sig для Excel, user_id внутренний); `--send` и таймер
  `tg-music-analytics-report` — отчёт за 7 дней дежурному админу по понедельникам
  09:00 МСК, длинный текст режется по строкам под лимит Telegram. Выкачено
  (`1edbd7f`): таймер включён, ближайший запуск 21.09 06:00 UTC; CSV на проде
  выгружается.
- **«Давно не виделись» (`comeback`)** — по первому отчёту: удержание D1 9%, D7
  3%, а напоминания о Premium доходят только до людей с Premium. Разовое
  сообщение за жизнь аккаунта тем, кто без Premium и молчит **30–180 дней**
  (владелец 16.09: «должно быть редким — вот если 30 дней пройдёт»; дольше
  полугода — человек бота забыл, это спам). Не бравшим пробную неделю — про 7 дней
  бесплатно, остальным — «напишите название трека» + друзья. Кнопки те же. Не
  больше 30 за ночь (владелец 16.09: «главное не спамить»), первыми — недавно
  пропавшие. Активность — по
  `track_events`, `search_queries`, `funnel_events`, `analytics_events` (кроме
  `system`). Выкачено (`6720e37`), `--dry` на проде: **26 к отправке** (24 —
  про пробную неделю, 2 — «напишите название трека»; ru 25, en 1; тишина 3–28 дней — по прежнему правилу «от 3 дней»). Первый реальный запуск — таймер обслуживания 16.09 04:00 UTC.
- **Кнопка «📊 Аналитика» в `/admin`** (`35a89fe`): тот же отчёт за 1/7/30 дней в
  боте, длинный — несколькими сообщениями.
### Ночной нагрузочный тест прода (17.09, 00:01–00:04 UTC, окно владельца)

`scripts/load_test_prod.py`, ступени 5 → 20 → 50 клиентов по 30 сек, с машины владельца
(латентность включает его сеть ~0,2 сек), смесь: каталог, id библиотеки, полки, статика.

| клиентов | запросов | p50 | p95 | max | 200 | 429 | 5xx / обрывы |
|---|---|---|---|---|---|---|---|
| 5 | 364 | 250 мс | 1422 мс | 2422 мс | 165 | 199 | 0 / 0 |
| 20 | 2053 | 250 мс | 500 мс | 735 мс | 247 | 1806 | 0 / 0 |
| 50 | 3849 | 312 мс | 797 мс | 2047 мс | 459 | 3390 | 0 / 0 |

- ✅ **Сервер не падает и не сыплет 5xx**: лишнее режет ограничитель (120/мин на человека,
  600/мин на адрес). Load average весь тест ~0,1, API держался на 95 МБ.
- ⚠️ **Ёмкость так не измерить**: с одного адреса к API проходит ~10 запросов/сек — ровно
  потолок ограничителя. Ослаблять защиту на проде ради замера не стали. Честный замер —
  только с многих адресов или копией прода на отдельной машине.
- 🔴 **Находка — не нагрузка, а соседи по машине.** Во время теста доступная память упала
  379 → 69 МБ не из-за нас: около 23:50 перезапустились `news-rewriter-bot` (166 МБ) и
  `yt-vk-autopost` (55 МБ) — чужие проекты на том же боксе. Своп занят на 1 ГБ, и наш
  поисковый воркер `tg-music-youtube-user` лежит в свопе на **207 МБ из 222** (в RAM 15 МБ):
  первый поиск после простоя ждёт подкачки с диска. Решение за владельцем — это не наш код.

### Автопроверка бэкапа и замер после отключения соседей (17.09, 00:20 UTC)

- ✅ **`cb26efa` выкачен** (00:19:10). Ночной бэкап теперь сам проверяет снимок на
  временной копии: целостность, миграция = живая база, люди/треки есть и не меньше 90%
  живой базы; при сбое — тревога владельцу. Прогон на настоящем снимке
  `db-20260916-040002.sqlite`: целостность ok, миграция `9c8d7e6f5a4b`, users 69, tracks 7589.
- ✅ **Владелец остановил `news-rewriter-bot` и `yt-vk-autopost`** (оба `inactive`). Своп
  949 → 464 МБ, load average 0,2. Доступно ~100 МБ — сразу после рестарта наших сервисов
  выкаткой, значение ещё устаканится. Крупнейшие: поисковый воркер 228 МБ, бот 217 МБ,
  воркер 78 МБ, веб-админка 68 МБ, API 31 МБ.
- ⚠️ **Задержка с машины владельца не изменилась.** С сервера TLS 0,03 сек. С машины
  владельца TCP до сервера 6–90 мс, TLS 1,3–6,6 сек, первый запрос 12,6 сек. TCP за 6 мс
  до зарубежного сервера физически невозможен — соединение по-прежнему перехватывает
  что-то локальное (VPN/антивирус/фильтр провайдера). Со стороны сервера чинить нечего.

### Прогрев по спискам владельца (21.09) — и почему список нельзя брать на веру

Владелец прислал два списка популярной музыки: 198 артистов по 5 треков, затем
~320 артистов по 10. Задача — прогреть их заранее, чтобы такие треки уходили
мгновенно по file_id.

- 🔴 **Половина второго списка — выдумка нейросети.** У десятков разных артистов
  повторялись одни и те же «песни»: «Город», «Капли», «Окна», «Шаг», «Навсегда»,
  «Дождь». А `find_track` при слабом совпадении отдаёт ВЕРХ выдачи (последний
  рычаг «лучше хоть что-то, чем пусто»), то есть по несуществующему названию в
  каталог лёг бы чужой трек под именем артиста. Поэтому прогрев переведён на
  имена: топ-10 треков артиста спрашиваем у источника
  ([data/popular-artists.txt](../../data/popular-artists.txt), 375 имён).
- 🔴 **Имя артиста по словам совпадает не с тем.** Живые промахи первой партии:
  по «Би-2» приехал «2z ft. Young H, Black Bi» (совпало слово `bi`), по «МакSим»
  — техно-продюсер «Maksim Dark» (имя оказалось началом чужого). Готовые
  `artist_hit`/`artist_affinity` тут не годятся — они и дают 1.0 в обоих
  случаях. Сверка имён целиком, сжатых до букв и цифр: равны; одно внутри
  другого при длине не меньше 0.6 от второго; либо похожи на 85%. Это разводит
  «GAYAZOV$ BROTHER$» и «GAYAZOVS BROTHERS» (один артист, 0.93) от «Би-2» и «2z…»
  (0.2). Плюс отсев junk-названий и повторов одной песни перезаливами.
  ⚠️ Побочно: «Кино Виктор Цой» в списке пришлось сократить до «Кино» — длинная
  форма отсекала настоящие треки группы.
- **Темп — решение владельца:** один трек в 30–60 секунд, весь список не быстрее
  двух недель. Отсюда отдельный `tg-music-warmup.timer`: раз в час ОДИН артист,
  10 треков по 45 сек ≈ 8 минут работы и 52 минуты тишины, 375 артистов ≈ 15 дней.
  Ночного рывка больше нет. Прогретых артистов помнит `--state` вне репозитория
  (деплой делает `git reset --hard`).
- ⚠️ **22 чужих трека из первой партии владелец решил оставить** — ни один не в
  библиотеках и не в плейлистах.
- ⚠️ **Тест админки был миной с таймером**: `premium_until` считался от жёсткой
  даты 16.09 + 5 дней, и ровно 21.09 проверка «premium_active is True» упала на
  ровном месте. Даты в фикстурах — только относительные.

### 🔴 «Треки очень долго присылаются» (21.09) — 7 сек ни на что + моя же предзагрузка

Жалоба владельца: «надо, чтобы бот хотя бы за 5 секунд находил и присылал».
Разбор дал две причины, и первая — регрессия от предзагрузки 19.09.

**1. Предзагрузка занимала оба потока воркера.** Замок слота жил 90 сек, а
работа шла до 260 сек (три скачивания по yt-dlp): замок истекал НА ХОДУ, вторая
предзагрузка занимала второй поток, и нажатие человека ждало в очереди.
Журнал: `search.fetch_candidate` получена 07:19:02, отработала 07:22:14 —
**3 минуты в очереди** при собственной работе 0,4 сек.
Лечение: замок 600 сек с продлением между треками + предзагрузка прекращается,
как только `llen(youtube_user) > 0`, то есть кто-то уже ждёт нажатый трек.
⚠️ Урок: фоновая работа, делящая пул потоков с тем, что ждёт человек, обязана
уметь уступать. Замок «на всякий случай» тут не защита, а иллюзия: он должен
переживать всю работу, иначе просто удваивает параллелизм.

**2. yt-dlp тратил 7,0 сек на то, что едет 0,15 сек** (замер прода, `kizaru — Fake ID`):

| шаг | yt-dlp | прямой путь через API v2 |
|---|---|---|
| разбор страницы / метаданные | 1,23 сек | resolve 0,20 + карточка 0,06 |
| получение ссылки на поток | внутри | 0,07 сек |
| скачивание 3,4 МБ | 0,15 сек (22 МБ/с) | 0,15 сек |
| **итого** | **7,00 сек** | **0,5 сек** |

У SoundCloud среди вариантов потока есть `progressive` + `audio/mpeg` — готовый
mp3, который не надо ни склеивать из кусков HLS, ни перекодировать ffmpeg
([soundcloud_fast.py](../../app/services/soundcloud_fast.py)). yt-dlp остался
запасным путём: HLS-только треки, DRM, YouTube.

**Замер после выкатки `443213d`** (от запроса до готового к отправке трека):

| запрос | поиск | скачивание + теги + минт | итого |
|---|---|---|---|
| macan бенз | 0,45 сек | 1,67 сек | **2,14 сек** |
| drake one dance | 0,55 сек | 2,21 сек | **2,79 сек** |
| luv resval oblivion | 0,47 сек | 1,47 сек | **1,97 сек** |
| три дня дождя (уже в базе) | 0,46 сек | 0,45 сек | **0,95 сек** |

- ✅ Требование владельца «хотя бы 5 секунд» выполняется с запасом.
- ⚠️ Платное качество тут ни при чём: `audio_quality` у всех 74 пользователей
  `mp3`, ветка `deliver_best_quality` не срабатывает.
- ⚠️ «нурминский цепи» прямым поиском SoundCloud даёт 0 кандидатов — в боте
  запрос идёт шире (транслитерация + YouTube), но само по себе это напоминание,
  что замеры скорости надо делать на запросах, которые точно находятся.

### Скорость выдачи и скрины владельца (19.09)

Эталон владельца — видео с @RuMediaBot: запрос «Kizaru» → список за ~1 сек, тап
по номеру → аудио в чате за 1–2 сек. У нас список такой же быстрый (0,22 сек
поиск), а вот трека, которого нет в базе, ждут ~8 сек: скачивание с минтом идёт
ПОСЛЕ нажатия.

- **Предзагрузка верха выдачи** ([prefetch.py](../../app/services/prefetch.py),
  задача `search.prefetch`): первые 3 кандидата (решение владельца) качаются,
  пока человек читает список. Нажали — трек уже в базе и уходит по file_id.
  - ⚠️ Одновременно идёт ОДНА предзагрузка на весь бот (общий замок в Redis):
    у воркера два потока, и второй обязан оставаться свободным для того, что
    человек нажал сам. Без Redis предзагрузки нет вовсе — иначе она заняла бы оба.
  - ⚠️ Нажатие на кандидата, который сейчас качается заранее, НЕ ставит второе
    скачивание: бот ждёт первое (`_wait_prefetched`, до 25 сек, опрос раз в
    0,5 сек). Ждём в боте, а не в воркере: это `asyncio.sleep`, он ничего не стоит.
  - ⚠️ Побочно: каждый заранее скачанный трек — это пост в архивном канале
    (минт file_id), то есть до трёх постов на поиск вместо одного.
- **Обложки**: у SoundCloud размер задаётся суффиксом файла. Замер 19.09:
  `-original` до 800 КБ — и именно его грузили в строку списка на 46px (3506
  треков из 7600); `-t500x500` в плеере на экране ×3 выглядел мыльно;
  `-t1080x1080` есть у всех и весит 240 КБ. Теперь плеер берёт 1080, списки 300.
- **«Квадрат больше картинки»** (скрин владельца): экран плеера — flex-колонка,
  и когда под обложкой появляется ряд кнопок «Скачать / Микс по треку», колонка
  ужимала обложку по ВЫСОТЕ. Рамка с `aspect-ratio: 1` оставалась шире картинки,
  а `object-fit: contain` дорисовывал полосы. Лечение: `flex-shrink: 0` на
  обложке, прокрутка у экрана плеера, `object-fit: cover` (решение владельца —
  заполнять квадрат). Размытая подложка удалена. На десктопе бага не было —
  там высоты хватало.
- **«Неизвестный» без обложки** оказался не треком, а МИНУСОМ из ТГ-канала: у
  минусов в базе не было колонки обложки вовсе, а исполнитель «Неизвестный» у 20
  из 491. Добавлены `instrumentals.cover_url` и `enrich_checked_at` (миграция
  `a7b8c9d0e1f2`), ночная задача `app.cli.enrich_minuses --limit 40` ищет
  недостающее в SoundCloud (порог совпадения 0.6 — выше обычного: ошибка видна
  каждому в плеере). Исполнителя пишем только там, где его нет.
- **«Плейлисты» висят на «Загружаю…»**: у запросов Mini App не было предела
  ожидания — зависшее соединение держало экран вечно. Теперь 20 сек, отказ и
  кнопка «Повторить» (раньше ошибка показывалась как «Плейлистов пока нет»).
- **Тексты песен**: смотреть может любой вошедший, добавлять и править — только
  админы (решение владельца). Раньше правил любой Premium, а текст общий для всех.

### Бэкап и «медленная главная» (16.09) — проверено, не гадание

- ✅ **Восстановление из бэкапа проверено впервые.** Снимок `backups/db-20260916-040002.sqlite`
  (13,7 МБ) скопирован во временный файл: `PRAGMA integrity_check` — ok, миграция
  `9c8d7e6f5a4b` = прод, приложение и `alembic current` работают поверх копии, счётчики
  таблиц совпадают с продом (расхождение — только записи после 04:00). Копия удалена.
- 🔴 **Риск: бэкапы на том же диске, что база** (`BACKUP_KEEP=2`). Смерть диска или сервера
  уносит и базу, и оба снимка. Нужна копия вне сервера — решение за владельцем.
- **«Главная 3,2 сек» — не сервер.** С самого сервера TLS 0,03 сек, первый байт 0,036 сек.
  С машины владельца TCP 11–30 мс (до зарубежного сервера так не бывает — соединение
  принимает локальный VPN/прокси/фильтр провайдера), а TLS 0,5–2,7 сек. Конфиг TLS в
  порядке: 1.2/1.3, ECDSA-сертификат, кэш сессий. Со стороны сервера чинить нечего.

### Первая рассылка «давно не виделись» (16.09, 04:00 UTC)

- Правило «после 30 дней тишины», потолок 30 за ночь. **К отправке 30: дошло 15,
  ещё 15 уже заблокировали бота** — теперь помечены `bot_blocked`, писать им больше
  не будем (всего заблокировавших 8 → 23). Сбоев 0.
- 🔴 **Вывод для продукта:** половина «молчащих месяц» не молчит, а ушла насовсем.
  Возврат за 48 часов смотреть в отчёте аналитики ~18.09 (строка «напоминания»).
- Код выхода 1 у ночного обслуживания — не сбой: все шаги отработали, «1» вернула
  последняя проверка выходов VPN (все 5 мертвы, ждём новые узлы от владельца).
- Ночной нагрузочный тест в окно 16.09 не прогнан: сессия простояла до 08:39 UTC.

### Mini App одним файлом и веб-админка (16.09)

- **Сборка `tools/build_miniapp.py`**: 47 модулей и 4 таблицы стилей склеиваются в
  `miniapp/app.bundle.js` (75 КБ сжатым) и `app.bundle.css` (17,5 КБ). Каждый модуль
  в своей области видимости, порядок топологический, цикл импортов — ошибка сборки.
  Собранные файлы лежат в репозитории: деплой это `git pull`, сборщика на сервере нет.
  Сторож — `tests/test_miniapp_bundle.py`.
  🔴 **Грабля:** отпечаток исходников считался по байтам, у Windows CRLF, у CI LF —
  CI отклонил выкатку (и правильно). Считаем по тексту, пишем файлы с `newline="
"`.
- **Найдено сборкой:** первая пачка аналитики уходила до входа в приложение и
  терялась насовсем. Теперь неотправленная пачка возвращается в очередь.
- **Веб-админка** (`app/webadmin/`, `webadmin/`, [docs/АДМИНКА.md](../АДМИНКА.md)):
  на сервере слушает только `127.0.0.1:8011`, в nginx не заведена; владелец
  открывает её у себя `python -m app.webadmin` через SSH-туннель. Разделы: обзор с
  графиками, люди, каталог, деньги. Только чтение — правки и рассылки в боте.
  Тесты поймали два бага сразу: подстановка суммы донатов вместо их числа и поиск
  по русскому имени (SQLite lower() не понижает кириллицу — третий случай той же грабли).

- Мусор в каталоге проверен и **не чистился**: из 7589 треков 6 с `.m4r`/`http`
  в названии, 1 с префиксом «[МИНУС]», 1 с названием >120 символов. В топ
  попадают только потому, что слушателей мало; отчёт режет имена до 60.

### Очередь следующих циклов (не сделано на 14.09)

Брать сверху. Каждый пункт — найден, но не исправлен или не измерен.

1. **Ночной нагрузочный тест прода** (разрешён владельцем 03:00–05:00 МСК, без
   рассылки). Локально p50 1.1 сек при 300 параллельных — на проде UNKNOWN.
   🔴 **Замер реального трафика 15.09 — важнее нагрузки.** nginx за 14.09: `GET /`
   Mini App 161 раз с 102 IP, но это в основном сканеры (Censys, masscan, Palo
   Alto, UA «iPhone OS 13_2_3»); `/src/main.js` — 10 загрузок, **все от сканеров**
   (research-scan, «Opera 7.10» и т.п.), живых открытий Mini App — 0;
   `/api/login` — **0** (за 04–13.09: 0–8 в день). Бот за 14 дней: события у
   1–3 человек в день, 22 поисковых запроса от 9 пользователей. В API за сутки 94 запроса, из них реальных
   клиентских — нет (audio — мои проверки, остальное — пробы `.env`/phpunit).
   База: 69 пользователей, активных по событиям за 24 ч — 1, за 7 дней — 3,
   новых за 7 дней — 9, Premium — 1. Вывод: узкое место продукта сейчас не
   производительность, а приток и удержание (iOS-предупреждение Safari,
   пэйвол Mini App). Нагрузочный тест на 1 млн пользователей это не отменяет, но
   по приоритету ниже.
2. ~~**Холодный старт 4.3 сек**~~ ✅ **закрыт замером 15.09** (`/root/coldstart_probe.sh`,
   после цикла 7). API отвечает `/health` через **3.4 сек** после restart, первый
   запрос свежего процесса — **64 мс** (`/library/ids`), повтор 7 мс. Гипотеза
   «кэш страниц SQLite» **не подтвердилась**: база на проде 13.7 МБ, выброшенный
   из кэша файл (`posix_fadvise DONTNEED`) даёт +10 мс, прогрев `cat` — ничего.
   Прогрев в деплой НЕ добавлен. 4.3 сек платил свежий процесс с aiogram в
   импорте при забитом свопе — ушло вместе с aiogram.
3. **Поиск на масштабе**: `ilike '%q%'` — полный скан. На 7.4 тыс. треков
   незаметно, на 1 млн неприменимо → план SQLite FTS5.
   **Замер прода 15.09 (7589 треков):** `/tracks?q=` и `/search/all?q=` —
   32–68 мс на тёплом («к», «kizaru», «макан»), разовый холодный первый запрос
   1.37 сек. ⏸ **Отложено осознанно:** скан линейный, порог боли (~0.5 сек)
   ожидается около 100 тыс. треков, а каталог сейчас растёт живым поиском на
   десятки в день. Вернуться, когда `select count(*) from tracks` > 50 000.
   ⚠️ FTS5 `unicode61` понижает кириллицу сам — это заодно закрыло бы грабли
   с SQLite `lower()`.
4. **Mini App без сборщика**: 46 ES-модулей = десятки запросов на старте.
   Сборка в один файл или шире `modulepreload`.
5. ~~**`/upload`** держит до 50 МБ в памяти~~ ✅ **цикл 8, 15.09.** Найдено хуже,
   чем записано: файл лежал ДВАЖДЫ (`bytearray` + `bytes(buffer)`, пик ~100 МБ),
   а 🔴 длительность (mutagen), отпечаток (`fpcalc` через `subprocess.run`) и
   запись в хранилище шли синхронно в event loop — пока одна загрузка считала
   отпечаток, Mini App стоял у всех. Теперь поток пишется во временный файл с
   остановкой на потолке, длительность и отпечаток — по пути в пуле потоков,
   байты поднимаются один раз (для хранилища), `storage.save` — в потоке,
   временный файл удаляется и при отказе. 2 слота оставлены. Стерегут
   `test_api_payloads.py` (отпечаток вне event loop, временных файлов нет,
   потолок → 400).
6. ~~Автодеплой GitHub Actions падает на SSH~~ ✅ 15.09: заменён pull-деплоем с
   сервера (тесты GitHub по публичному API → `remote-deploy.sh`). Тесты в CI
   были зелёными все 25 раз, падал только SSH-шаг. Осталось владельцу: удалить
   неиспользуемый SSH-ключ деплоя из `authorized_keys` и секреты `VPS_*`.
6а. **Переезд домена** (владелец 15.09: «Deceptive Website Warning» и в Safari,
   и в Telegram на iOS; Google Safe Browsing при этом показывает keybest.cc,
   www и пути чистыми). ⏳ **Ждём домен от владельца — ориентир ~20.09**
   (тогда же удалить секреты `VPS_*` в GitHub): купить,
   лучше .ru/.com (не .cc), A-запись и www → 38.244.213.132. Готово к одной
   команде: `bash deploy/migrate-domain.sh --check <домен>`, затем без `--check`.
   Скрипт переписан: проверки DNS/порта 80/токена без изменений (`--check`),
   замок деплоя, бэкап `.env` в `/root/env-backups`, HTTP/2 после certbot,
   кнопка Mini App через Bot API `setChatMenuButton`, рестарт всех читающих
   `PUBLIC_BASE_URL`. Файлы репозитория на сервере НЕ правит (автодеплой делает
   `git reset --hard`). Домен убран из кода и статики Mini App —
   `tests/test_no_hardcoded_domain.py`. После переезда руками: вебхук ЮKassa,
   проверка на iPhone; `deploy/nginx-keybest.conf` и доки — коммитом.
7. ~~**8 копий `.env.bak-*` с секретами** в `/opt/tg-music-bot`~~ ✅ 15.09 (владелец:
   «сделай сам»): перенесены в `/root/env-backups` (каталог 700, файлы 600), не
   удалены. В каталоге проекта остались только `.env` и `.env.example`. Там же
   убран ключ старого SSH-деплоя из `authorized_keys` (бэкап —
   `/root/ssh-backups/`, файлы ключа — `/root/old-deploy-key/`).
8. ~~План масштабирования 100 → 1 млн пользователей~~ ✅ [SCALING-PLAN.md](SCALING-PLAN.md).
   Из него в очередь: ~~аудио через nginx `X-Accel-Redirect`~~ ✅ цикл 6.
9. ~~**API держит ~180 МБ анонимной памяти**~~ ✅ **цикл 7, 15.09, `8c85828`.**
   Сделано: `services/bot_api.py` (aiohttp: getChatMember, getMe, getFile +
   скачивание; токен не попадает в текст ошибок), `tasks/queue_client.py`
   (`send_task` по имени без импорта модулей задач), вынесены
   `track_lookup/metadata.py` и `playlist_transfer/locks.py`, aiogram в
   `catalog_import`/`subscription` — только лениво или в TYPE_CHECKING.
   Статусы участника сравниваются строкой: ⚠️ у enum aiogram хэш от ИМЕНИ,
   `ChatMemberStatus.MEMBER in {"member"}` молча даёт False.
   Замеры: Python-аллокации импорта 91.5 → **58.2 МБ**; прод, API (RssAnon+VmSwap)
   ≈186 МБ до → **≈76 МБ** после свежего старта; aiogram в `/proc/PID/maps` — 0.
   Выкатка автодеплоем: 0 ошибок / 0 5xx, аудио 206/403/404 проверено.
   Стерегут: `test_api_process_imports.py` (API не грузит aiogram; имена и
   очереди задач = воркер), `test_bot_api.py`. yt-dlp (+9 МБ) в API
   оставлен **осознанно**: первым его импортирует `services/soundcloud.py`, но
   `/stream/{ref}` (`stream_url.resolve_stream_url`) зовёт yt-dlp на КАЖДОЕ
   воспроизведение из живого поиска — ленивый импорт лишь отложил бы 9 МБ до
   первого такого запуска. Не делать.
   Исходный замер импортов (`tracemalloc`, аллокации Python, итого 91.5 МБ):
   **aiogram.types+methods +38.4 МБ (42%)**, fastapi+sqlalchemy+pydantic
   +26.5 МБ, остальной код +17.4 МБ, yt-dlp всего +9.2 МБ. По времени импорта
   (`-X importtime`) aiogram — 6.5 из 8.6 сек. ⚠️ Первая гипотеза «виноват
   yt-dlp» не подтвердилась. Рычаг — убрать aiogram из процесса API: заменить
   `Bot` прямыми вызовами Bot API через aiohttp (getChatMember, sendAudio,
   getFile). Ленивый импорт памяти не сэкономит: гейт подписки зовёт Telegram
   почти у каждого, модели загрузятся на первом же запросе.
   **Объём (grep 14.09)** — aiogram попадает в процесс API так:
   - роутеры `api/routers/audio.py` (`Bot.download`), `contests.py` и
     `subscription.py` (`Bot` → `getChatMember`);
   - `services/subscription.py` (`Bot`, `ChatMemberStatus`, `TelegramAPIError`,
     `ChatMemberRestricted`);
   - `services/track_lookup/importer.py` — API нужна оттуда одна
     `candidate_metadata`, а `Bot` висит в верхних импортах модуля;
   - `services/catalog_import.py` (`Bot`, `BufferedInputFile`) — путь `/upload`.
   Сервисы общие с ботом и воркерами → отдельный цикл: лёгкий клиент Bot API +
   вынос `candidate_metadata` в модуль без aiogram + тест «импорт `app.api.app`
   не загружает `aiogram`» (`sys.modules`).
- 🔴 **`ensure_user` писал в SQLite на каждое действие в боте**: `get_or_create_user`
  безусловно ставил `last_login = now()` и делал `commit()` — пишущая транзакция
  на каждое сообщение и нажатие, в очереди за единственной блокировкой писателя
  вместе с API и двумя воркерами. Теперь commit только при изменении: новый
  пользователь, сменились username/имя/язык, или `last_login` старше 10 минут
  (его читает лишь статистика «заходил хоть раз» — `IS NOT NULL`).
- ✅ Выкатка `c48a7eb`: 888 тестов, наблюдение 0 ошибок / 0 ответов 5xx, в
  журналах бота и API после рестарта ошибок нет.

### Цикл 6 — 14.09: аудио через nginx (X-Accel-Redirect)

- Задача из [SCALING-PLAN.md](SCALING-PLAN.md): даже кусками по 256 КБ байты
  аудио гонит Python — итерации event loop и поток из пула на каждый кусок, на
  единственном ядре. API теперь проверяет подпись и отвечает заголовком
  `X-Accel-Redirect: /_audio_cache/tracks_<id>`, файл и Range отдаёт nginx
  (sendfile). Включается `AUDIO_ACCEL_REDIRECT_PREFIX=/_audio_cache/`, пусто —
  прежний стрим из API. Только для файлов прямо в каталоге кэша.
- На проде добавлен `location /_audio_cache/ { internal; alias …; }` (бэкап в
  `/root/nginx-backups`, `nginx -t` ок). Прямой запрос снаружи — 404.
- Проверка — `/root/accel_verify.sh` на сервере (только чтение). **Базовая линия
  до включения** (стрим из Python): mp3 `audio/mpeg`, m4a `audio/mp4`; Range
  `0-1023` и `-100` — 206, байты совпадают с файлом; 8 параллельных полных
  загрузок — RSS API 81 → 83 МБ; подделанная подпись — 403.
- ⚠️ Что сверить после включения: m4a обязан остаться `audio/mp4` — у
  internal-location `default_type audio/mpeg`, и если nginx не возьмёт тип из
  ответа API, Safari может отказаться играть.
- ✅ **Включено на проде** (`f943b3f`, `AUDIO_ACCEL_REDIRECT_PREFIX=/_audio_cache/`,
  бэкап `.env` в `/root/env-before-accel-*`). Проверка после: m4a остался
  `audio/mp4`, mp3 `audio/mpeg`; Range `0-1023` и `-100` — 206, байты совпадают;
  `X-Accel-Redirect` наружу не утекает; подделанная подпись 403, internal 404;
  в журнале API аудиозапросы 200. Наблюдение деплоя: 0 ошибок, 0 ответов 5xx.
- ⚠️ `Accept-Ranges` приходил дважды (API + nginx) → убран из ответа API.
  Выкатка `d91f81b`: наблюдение 0/0, заголовок один, m4a `audio/mp4`, Range и
  байты совпадают, 403/404 на месте.
- RSS API 203 МБ сразу после рестарта против 81 МБ до включения — **не
  регрессия**. Проверено: `NRestarts=0`, OOM нет; «uptime 1 минута» — это мой же
  рестарт при включении переменной (09:58:05). Через 5 минут у процесса уже
  11 МБ в свопе: прежние 81 МБ мерились на долго живущем процессе, частично
  выгруженном в своп (RSS своп не считает). Во время 8 загрузок RSS не рос —
  байты идут мимо Python. `MemoryPeak` 230 МБ.
- Откат одной командой без деплоя: убрать строку из `.env` и
  `systemctl restart tg-music-api`.
