# СТАТУС ПРОЕКТА ПО СВОДУ

Применение [AI Engineer OS](README.md) к Telegram Music (@muz_damn_bot + Mini App
на keybest.cc). Обновляется по итогам каждого аудита.

**Последнее обновление:** 2026-09-14
**Метод:** чтение кода и сверка с первоисточниками. Замеры на проде в этом
проходе не делались — соответствующие пункты помечены `UNKNOWN`.

---

## Infinity QA v2.0 — бесконечный цикл (с 2026-09-14)

Решения владельца и журнал циклов — [docs/qa/STRESS-TEST.md](../qa/STRESS-TEST.md).

| # | Находка | Серьёзность | Статус |
|---|---|---|---|
| 1 | Пэйвол Mini App только в интерфейсе: весь API доступен бесплатному | Critical (деньги) | ✅ `221373f`, прод проверен |
| 2 | Тест обхода маршрутов не видел роутеры (`_IncludedRouter`, FastAPI 0.141) | High (ложный PASS) | ✅ |
| 3 | Гейт подписки поверх пэйвола обещал бесплатный доступ | Medium (UX, враньё) | ✅ |
| 4 | Ошибки пользователей на проде никто не видел, деплой проверял только `is-active` | High (эксплуатация) | ✅ сторож + наблюдение после деплоя |
| 5 | Первый запрос после рестарта 4.3 сек | Low | UNKNOWN причина, наблюдается |

---

## Проход 2026-09-13 — стресс-тест и злоупотребления (проходы 4, 6, 7)

**Метод:** скрипт в обход интерфейса против локального API (35 проверок),
браузер на 375/768/1280, мягкие пробы прода по HTTPS без нагрузки. Нагрузку на
прод не давали: бокс 1 ядро / 961 МБ.

**Исправлено** (тесты — `tests/test_qa_stress_fixes.py`):

| Сев. | Находка | Правка |
|---|---|---|
| HIGH | XSS: названия треков из чужого плейлиста Spotify/Яндекса вставлялись в HTML экрана переноса | экранирование preview, ошибки, тоста, textarea |
| HIGH | Текст песни перезаписывал любой, в том числе бесплатный аккаунт; размер не ограничен (5 МБ проходили) | Premium на сервере, потолок 20 000 знаков |
| HIGH | Перенос без потолка занимал поток поискового парсера на часы | потолок 1000, отдельная очередь `celery`, один перенос на человека |
| HIGH | Скидка гасилась при создании ссылки, а не при оплате | гасится в `apply_succeeded_payment` по `metadata.discount_pct` |
| HIGH | Любой перерендер стирал ввод в загрузке, переносе, тексте, названии плейлиста | восстановление черновиков в `render()` |
| MEDIUM | Параллельное добавление в библиотеку, плейлист, подписку → 500 | `IntegrityError` = «уже есть» |
| MEDIUM | Триал: 7 активаций из 10 параллельных | условный `UPDATE … WHERE trial_used=0` |
| MEDIUM | Сироты: трек в плейлисте и подписка на несуществующие id | проверка существования, 404 |
| MEDIUM | Накрутка кликов по каналу: +30 за 30 запросов | один клик на человека в час |
| MEDIUM | Каждая перемотка live-потока заново запускала yt-dlp; у апстрима не было таймаутов | кэш ссылки 10 мин, таймауты |
| MEDIUM | Загрузка: пробелы вместо названия или битый mp3 → 500; файл целиком в памяти до проверки размера | валидация, чтение кусками |
| MEDIUM | Перенос сверял кириллицу через `lower()` и заново качал уже имеющиеся треки | `search_index` |
| MEDIUM | Кнопки плеера у live-трека были мёртвыми (id `live:…` → NaN) | не рисуются |
| LOW | `Range: bytes=-N` отдавал начало файла | суффиксный диапазон |
| LOW | id шире int64 → 500 (API и deep-link бота) | 404 / регулярка |
| LOW | Каждый «следующий» отправлял /listen: 60 тапов = 61 запрос | засчитывается после 5 сек звучания |
| LOW | Двойные тапы: оплата, «Ещё», «Скачать всё» | защита от повтора |
| LOW | «Скачать всё» брало только первые 100 треков | догрузка всех страниц |
| LOW | 429 без `Retry-After`; шапка «TG MUSIC»; тост «3 дня» при триале в 1 день; нет HSTS и `server_tokens off` | исправлено |

**Инфраструктура, найдено по ходу выкатки:**

- 🔴 **Автодеплой не работает минимум с 10.09** (три пуша подряд). Тесты на
  раннере зелёные, падает шаг «Выкатка на VPS». В журнале sshd за это окно нет
  даже попытки входа с раннера, то есть отказ на сетевом уровне: IP раннера в
  fail2ban (173 бана, среди них адреса Azure и GCP), фильтр хостера или неверный
  секрет `VPS_HOST`. Точная причина — `UNKNOWN`: лог job требует авторизации в
  GitHub. Коммиты 13.09 выкатывались руками тем же `deploy/remote-deploy.sh`.
  Чинить владельцу: открыть лог run в Actions.
- `/etc/systemd/system/ssh.service` начинался с лишней «ч» (байты `D1 87`) перед
  `[Unit]`. systemd игнорировал всю секцию `[Unit]` и сыпал предупреждения.
  Байты удалены, бэкап `/root/ssh.service.bak-*`, sshd не перезапускался.
- В живой nginx добавлены HSTS и `server_tokens off` (бэкап в `/root/nginx-backups`).

**Известный риск, оставлен решением владельца:** пэйвол Mini App только
клиентский — API не проверяет Premium, и через скрипт им пользуются бесплатно.
Бот при этом отдаёт те же треки бесплатно.

**Не исправлено:** свайп вниз на вложенном экране у верхнего края делает «Назад»
(ТЗ §3 владельца) и может сработать при прокрутке текста песни. Оставлено как
задумано.

**UNKNOWN:** задержки API под нагрузкой на проде (локально 300 параллельных
запросов: p50 ~1 с, один процесс uvicorn, Windows — к проду не переносится);
реальные устройства iOS/Android; поведение бота под флудом (покрыто только
существующими тестами антифлуда, живого клиента нет).

---

## Сводка

| Модуль | PASS | Находки | UNKNOWN |
|---|---|---|---|
| 01 Безопасность | частично | 1 HIGH — **исправлено** | файлы, матрица прав |
| 02 API и БД | частично | — | планы запросов не перепроверялись |
| 03 Фронтенд | частично | — | живые устройства |
| 04 Telegram | **да** по initData | — | iOS Safe Browsing |
| 05 Деньги | частично | 2 HIGH + 1 MEDIUM — **все исправлены** | живой рекуррент |
| 06 Инфраструктура | частично | — | восстановление из бэкапа |
| 07 Юридическое | **нет** | 2 открытых вопроса | режим после переезда |

---

## Находки этого прохода

### [HIGH] Журнал выручки не идемпотентен — повторный вебхук удваивает платёж

**Где:** [app/services/revenue.py:18](../../app/services/revenue.py#L18) `record_payment`,
модель `Payment` — [app/db/models.py:297](../../app/db/models.py#L297)

**Суть.** `record_payment` вставляет строку безусловно. У `Payment.charge_id`
нет ни проверки на существующий, ни уникального индекса. ЮKassa повторяет
уведомление, пока не получит `200`, — повтор по уже обработанному платежу
добавит вторую строку в журнал.

**Как воспроизвести:** доставить `payment.succeeded` по одному и тому же
`payment_id` дважды в ситуации, когда защита выше по стеку не сработала (см.
следующую находку) — в `payments` появятся две записи на один платёж.

**Последствие:** завышенная выручка в админ-статистике. Деньги не теряются, но
отчёт врёт, а именно по нему принимаются решения.

**Показательно:** в соседней, более новой таблице `Donation` ровно этот урок уже
учтён — `payment_id` объявлен `unique` с комментарием «ключ идемпотентности».
В `payments` его не добавили.

**Исправление: ✅ применено.**
1. Проверен прод: в `payments` **0 строк**, дублей по `charge_id` — 0, значит
   уникальный индекс встаёт на чистых данных.
2. `record_payment` дедуплицирует по `charge_id` и возвращает `bool`.
3. Миграция `a1b2c3d4e5f8` — частичный уникальный индекс `ux_payments_charge_id`
   (только для непустого `charge_id`: у Stars он пуст). Проверена `upgrade` и
   `downgrade` на временной БД.
4. Тесты: [tests/test_payment_idempotency.py](../../tests/test_payment_idempotency.py).

⚠️ Уникальность именно в БД, а не только проверка в коде: повторы приходят
параллельно, и «проверил → вставил» между двумя обработчиками не защищает.

---

### [HIGH] Идемпотентность активации Premium ломается при двух платежах подряд

**Где:** [app/services/yookassa_payments.py:230](../../app/services/yookassa_payments.py#L230)

```python
subscription = await session.get(PremiumSubscription, user.id)
if subscription is not None and subscription.payment_id == payment["id"]:
    return True  # повторное уведомление — уже обработано
```

**Суть.** Защита от повтора смотрит только на **последний** платёж пользователя.
Она перестаёт работать, как только пришёл следующий.

**Как воспроизвести:**
1. Пользователь оплачивает 1 месяц — платёж `A`. Вебхук `A` обработан,
   `subscription.payment_id = A`.
2. Тот же пользователь оплачивает 12 месяцев — платёж `B`. Теперь
   `subscription.payment_id = B`.
3. ЮKassa повторно доставляет уведомление по `A` (повторы идут до успешного
   ответа, окно — часы).
4. Условие `payment_id == A` ложно → `activate_premium` вызывается **второй раз**
   по платежу `A`: пользователь получает лишний месяц, в журнал выручки
   ложится дубль.

**Последствие:** выдача оплаченного товара сверх оплаченного + искажение отчёта.
Это не кража у пользователя, а потеря на стороне сервиса.

**Правильная защита:** ключ идемпотентности — **сам факт «этот `payment_id` уже
применён»**, а не сравнение с текущим состоянием. То есть та же таблица-журнал с
уникальным `charge_id`: если вставка отбита уникальностью — операция уже была,
выходим. Это чинит обе находки одной правкой.

**Исправление: ✅ применено.** Идемпотентность переведена на журнал платежей
(`payment_already_recorded`). Прежняя проверка по состоянию подписки оставлена
**вторым рубежом** — для платежей, применённых до появления журнала: у них
строки в `payments` нет, и по одному журналу повтор был бы не виден.
Тест `test_replay_after_second_payment_does_not_grant_twice` воспроизводит
именно сценарий A → B → повтор A.

---

### [MEDIUM] Копейки отбрасываются при записи в журнал

**Где:** `apply_succeeded_payment` и `_apply_donation`, `int(float(amount_raw))`.

Сумма приходит строкой вида `"78.00"` и приводится к целому рублю усечением.
Для текущих тарифов (целые рубли) расхождения нет, но правило из
[05-PAYMENTS §4](05-PAYMENTS.md) нарушено: журнал денег должен быть точным.
При появлении нецелых сумм отчёт начнёт занижать выручку молча.

**Исправление: ✅ применено.** Введена `_amount_to_rub`: округление вместо
усечения плюс предупреждение в журнал, если сумма нецелая — это сигнал, что
появился тариф, под который журнал не рассчитан. Перевод журнала на копейки не
делался: все текущие суммы целые, и менять схему ради гипотетического случая
дороже, чем поймать его предупреждением.

---

## Что проверено и оказалось исправным

Не перепроверять без причины.

### initData — соответствует документации Telegram

[app/api/security.py](../../app/api/security.py) реализует ровно документированный
алгоритм: `secret_key = HMAC_SHA256("WebAppData", bot_token)`, `data_check_string`
из отсортированных полей через `\n`, сравнение через `compare_digest`.
**Свежесть `auth_date` проверяется после подписи** — порядок верный, на него есть
тест. Отсутствие `hash` → отказ. `PASS` по [04-TELEGRAM §1](04-TELEGRAM.md).

### Авторизация эндпоинтов

Из 60 эндпоинтов без зависимости `get_current_user` остаются 4, и каждый —
осознанно:

| Эндпоинт | Чем защищён |
|---|---|
| `POST /login` | точка входа, проверяет `initData` |
| `GET /tracks/{id}/audio` | HMAC-подпись ссылки со сроком |
| `GET /instrumentals/{id}/audio` | HMAC-подпись, **отдельное пространство имён** (подпись трека не подходит) |
| `GET /stream/{ref}` | подписанный ref со сроком |
| `POST /webhook/yookassa` | перепроверяет платёж запросом к API ЮKassa |

Роутер `catalog` закрыт зависимостью на уровне роутера
(`APIRouter(dependencies=[Depends(get_current_user)])`) — при беглом чтении
выглядит как «эндпоинты без авторизации», но это не так.

⚠️ Уточнение к записи в CLAUDE.md: там сказано «у всех эндпоинтов есть
`Depends(get_current_user)` — кроме вебхука». Точнее: **кроме вебхука и трёх
эндпоинтов выдачи медиа**, которые защищены подписью, а не токеном.

### Вебхук платежей

Телу уведомления не доверяют — статус перепроверяется у API ЮKassa.
Возврат (`refund.succeeded`) разбирается корректно: `object.id` — id возврата,
платёж берётся из `object.payment_id`, и он тоже перепроверяется.
Донат отделён от Premium меткой `metadata.kind` **до** любой ветки активации.

### Идемпотентность донатов

`Donation.payment_id` — `unique`, `record_donation` возвращает `None` на повторе.
Соответствует [05-PAYMENTS §3](05-PAYMENTS.md).

### Лимит частоты у API

`RateLimitMiddleware` подключён в [app/api/app.py](../../app/api/app.py).
Прежняя запись «у API лимитера нет» устарела.

### Потолок страницы

`page_size` ограничен сверху (`MINIAPP_MAX_PAGE_SIZE = 100`) в каталоге.

---

## UNKNOWN — проверить отдельно

| Что | Почему не проверено | Как проверить |
|---|---|---|
| Планы запросов после последних правок | нужен прод | `EXPLAIN QUERY PLAN` на горячих путях |
| Восстановление из бэкапа | ни разу не проверялось на практике | развернуть снимок на копии |
| Бэкап вне сервера | по записям — только на диске машины | подтвердить наличие внешней копии |
| Живой рекуррент ЮKassa | тестами не покрывается | реальная карта, владелец |
| Safe Browsing на iOS | нужен живой iPhone | открыть домен в Safari и через бота |
| Поведение Mini App на реальных устройствах | эмуляция не заменяет | проверка владельцем |
| Права по матрице (полная таблица) | матрица ещё не составлена | заполнить [01-SECURITY §2](01-SECURITY.md) и покрыть тестами |

---

## Открытые вопросы юридического слоя

1. **Уведомление регулятора об обработке персональных данных.** Сервис собирает
   данные пользователей Telegram и принимает деньги через российскую платёжную
   систему. С 30 мая 2025 года за неуведомление предусмотрен штраф, в том числе
   для самозанятых. Статус: **не подтверждено, что уведомление подано.**
   Это вопрос к владельцу, а не к коду.

2. **Основание для раздачи каталога.** Для музыкального сервиса это главный
   юридический риск — больше, чем персональные данные. Нужны: канал для
   обращений правообладателей, срок реакции, техническая возможность быстро снять
   объект. Статус: **не проработано.**
   Отдельно: при переезде на западную платёжную систему её требования к товару
   строже закона — подтверждать **до** переезда, иначе обслуживание не откроют.

3. **Переезд на `.com` и западную аудиторию** (решение владельца).
   Юрисдикция определяется аудиторией, получателем денег, сервером и контентом,
   а не зоной домена — см. [07-LEGAL §1](07-LEGAL.md). Чек-лист переезда — §7 того
   же файла. Прежние обязанности нужно закрыть, а не оставить.

---

## Факты с прода (2026-09-08)

| Показатель | Значение |
|---|---|
| Пользователей | 60 |
| С признаком Premium | 8 |
| Строк в журнале выручки `payments` | **0** |
| Донатов | 0 |

⚠️ Пустой журнал выручки при восьми Premium означает, что **ни одной покупки за
деньги через журналируемые пути не прошло**: премиум у этих восьми — от админского
обхода, наград, рефералов или пробного периода. Это не поломка кода, а состояние
бизнеса, и его стоит знать до планирования доработок монетизации.

## Ближайшие действия по приоритету

```
1. [✓]  Исправления платежей на проде — проверено 2026-09-11: индекс
        ux_payments_charge_id есть в прод-базе, код на последнем коммите.
2. [—]  Матрица прав + тесты на «нельзя» (01-SECURITY §2).
3. [—]  Проверить восстановление из бэкапа и наличие внешней копии.
4. [—]  Аудит загрузки файлов (01-SECURITY §8) — не проходился.
5. [—]  Юридические вопросы владельцу (пункты 1–2 выше).
6. [—]  Проходы 4, 6, 7 (злоупотребление, отказы, нагрузка) — не выполнялись.
```

## Тесты

`665 passed, 1 skipped` (из них 4 новых — на идемпотентность платежей).
⚠️ В CLAUDE.md значилось «668 тестов» — расхождение существовало до этой сессии;
фактическое число на текущем коде указано выше.
